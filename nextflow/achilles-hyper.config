
singularity {

  enabled = false
  commands = "--nv"

}

profiles {

    standard {
        process.executor = 'local'
    }

    cluster {
        process.executor = 'pbs'
        process.clusterOptions = "-V -l ngpu=1"
    }

}

params {

    dir          = "achilles_selection"
    classes      = ["host", "pathogen"]
    labels       = ["human", "zika"]
    outdir       = "achilles_hyper_1"

    window_max   = [2, 3]
    window_size  = [200, 400]
    window_step  = [0.1]
    window_scan  = [50]

    validation   = 0.3

    chunk_size   = 5000

    nb_rb        = [1]
    nb_lstm      = [1]
    blstm        = [false]
    dropout      = 0.2

    epochs       = 3
    batch_size   = 1
    threads      = 2

    eval_dirs    = ["/home/esteinig/code/achilles/tests/test_data/dir1", "/home/esteinig/code/achilles/tests/test_data/dir2"]
    eval_max     = 5

    pred_dirs    = ["/home/esteinig/code/achilles/tests/test_data/dir1", "/home/esteinig/code/achilles/tests/test_data/dir2"]
    pred_batch   = 10
    pred_slices  = [1, 5, 10, 15, 20]

}